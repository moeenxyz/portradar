# Changelog

## v1

Initial open-source release.

- native macOS menubar app
- lists listening local TCP ports
- resolves process, app, launchd service, and CLI project labels where possible
- supports `Force Close`
- supports `Stop Service` for launchd-managed services
- supports launch at login
- includes custom app icon and standalone app bundle build script
